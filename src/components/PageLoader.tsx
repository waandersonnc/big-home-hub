export function PageLoader() {
    return (
        <div className="h-full w-full min-h-[50vh] flex items-center justify-center animate-in fade-in duration-900 backdrop-blur-sm bg-background/30">
            {/* Carregamento totalmente invisível/psicológico */}
        </div>
    );
}
